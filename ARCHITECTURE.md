# 架构图 · meeting-minutes-agent

```
┌─────────────┐
│ 会议转录文本 │  (zoom/飞书/腾讯会议导出 .txt 或 .vtt)
└──────┬──────┘
       │
┌──────▼──────────────────────────┐
│  Strands Agent (Professional)    │
│  - system_prompt: 纪要秘书角色    │
│  - structured output: JSON schema │
│  - 冲突/待决项检测               │
└──────┬──────────────────────────┘
       │
       ├──► 摘要 / 决策  ──┐
       ├──► 行动项(owner,due) ──┤
       └──► 开放问题 ────────┤
                              │
                   ┌──────────▼──────────┐
                   │  渲染层 minutes.md   │
                   │  + 行动项可订阅提醒   │
                   └──────────┬──────────┘
                              │
                   ┌──────────▼──────────┐
                   │ 人工确认回路         │
                   │ (仅开放问题 ping 人) │
                   └─────────────────────┘
```

技术要点：
- 框架：Strands Agents SDK（AWS 开源），原生对接 Bedrock。
- 约束输出：JSON schema 保证纪要可被下游（日历/任务系统）消费。
- 静默原则：默认后台产出，仅当检测到"开放问题/冲突"才主动通知人。
- 可扩展：接 MCP 工具可自动建日历事件、写任务到项目管理器。
