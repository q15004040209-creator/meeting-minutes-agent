# Demo 视频拍摄脚本 · meeting-minutes-agent

> AWS Agents for Humans Hackathon · Professional Agents 赛道
> 目标时长：**4 分 00 秒**（赛规上限 5 分钟，留 1 分钟余量）
> 形式：屏幕录制 + 旁白（OBS / QuickTime / Loom 均可）。全程不需要露脸。
> 录制前准备：终端字号调到 18pt 以上（评委在小窗口里也要看得清）、准备一份 300 行左右的真实会议转录 `meeting.txt`、配好 `AWS_REGION` 与 Bedrock 模型访问权。

---

## 镜头 1 · 问题（0:00 – 0:35）

**画面**：一份原始会议转录文本，快速向下滚动，滚过十几屏还没到底。

**旁白**：
> "这是一场 50 分钟的项目会。转录出来 4000 多字。
> 会后要有人把它变成纪要——谁负责什么、什么时候交、哪些事定了、哪些还没定。
> 这件事每周都在发生，每次都要花 30 到 40 分钟，而且做的人通常是会上最忙的那个。
> 它重复、机械、有明确的输出格式——这正是 Agent 该接过去的活儿。"

**要点**：**先让评委相信这是真痛点**，不要一上来就讲技术。赛道叫 Professional Agents，评的就是"是否真的替人省下了重复劳动"。

---

## 镜头 2 · 一条命令（0:35 – 1:05）

**画面**：终端里敲下命令，回车。

```bash
python agent.py --transcript meeting.txt
```

**旁白**：
> "整个操作就这一条命令。
> Agent 用 Strands Agents SDK 构建，接管从原始转录到结构化纪要的全过程——
> 不需要我挑段落、不需要我提示它'找一下行动项'。"

**要点**：让评委看到**零人工介入**。这是"Agents for Humans"的核心命题。

---

## 镜头 3 · Agent 在干什么（1:05 – 2:00）

**画面**：终端实时输出 Agent 的推理与工具调用过程（保留 Strands 的 trace 输出，不要静默运行）。

**旁白**：
> "它在做四件事：
> 第一，分段——按话题边界切分，而不是按固定长度，因为一个议题常常横跨十几轮对话；
> 第二，抽取——决议、行动项、悬而未决的问题分三类抽，行动项必须带责任人和时间；
> 第三，消歧——转录里的'小王'和'王工'是同一个人，Agent 会做指代归并，这一步不做，行动项的责任人就会散成好几个；
> 第四，结构化校验——输出必须符合预定义的 JSON schema，缺字段就自己补一轮，不会吐出半成品让人再去清洗。"

**要点**：这一段是**技术深度分**。评委要看的是"Agent 真的在做 agentic 的事"（多步、有工具、有自我校验），而不是一次 prompt 调用套个壳。

---

## 镜头 4 · 结果（2:00 – 3:00）

**画面**：左边原始转录，右边生成的纪要，并排对照。鼠标指着行动项逐条划过。

**旁白**：
> "输出是结构化的：三句话摘要、决议清单、带责任人和截止日期的行动项、以及会上没能定下来的开放问题。
> 注意这一条——'库存接口的降级方案下周三前给出，负责人张工'。
> 这句话在转录里是散落在第 12 分钟和第 38 分钟两处的：一次提了任务，一次才补了时间。
> Agent 把它们合成了一条完整行动项。这是纯正则或者纯摘要模型做不到的。"

**要点**：**挑一个最能体现"跨段落合并"的例子**。评委一天要看几十个 demo，一个具体的、有说服力的细节比十句形容词有用。

---

## 镜头 5 · 架构与可复现（3:00 – 3:40）

**画面**：打开 `ARCHITECTURE.md` 的架构图，然后切到 GitHub 仓库页面。

**旁白**：
> "架构上，Strands Agents SDK 负责 agent loop 与工具编排，模型走 Amazon Bedrock，
> 结构化输出由 JSON schema 约束，失败自动重试。
> 代码全部公开在 GitHub，Apache 2.0 许可，README 里有完整的依赖与运行说明——
> 你现在就可以 clone 下来，用自己的会议录音跑一遍。"

**要点**：赛规要求公开仓库 + 架构图 + 许可，**在视频里明确展示出来**，帮评委完成合规勾选。

---

## 镜头 6 · 收尾（3:40 – 4:00）

**画面**：回到并排对照画面，定格。

**旁白**：
> "每周省下 40 分钟，一年是 30 多个小时。
> 更重要的是，纪要不再取决于'那天谁比较闲'——
> 它每次都在，格式一致，行动项不会漏。
> 这就是我们想让 Agent 替人接住的那类事。"

---

## 录制自查清单

- [ ] 视频总长 ≤ 5:00
- [ ] 全程能看清终端文字（字号 ≥ 18pt，1080p 以上）
- [ ] 展示了 Strands Agents SDK 的实际运行痕迹（不是只有最终结果）
- [ ] 展示了公开 GitHub 仓库 URL（可停留 3 秒让评委看清）
- [ ] 展示了架构图
- [ ] 至少有一个具体案例说明 Agent 做到了简单方法做不到的事
- [ ] 无敏感信息（真实公司名、人名、内部系统名 → 录制前替换为化名）
- [ ] 上传到公开可访问平台（YouTube 不设私密 / B站 / Loom），链接可匿名打开

## 提交表单要填的字段（Devpost）

| 字段 | 内容 |
|---|---|
| 赛道 | Professional Agents |
| 仓库 | https://github.com/q15004040209-creator/meeting-minutes-agent |
| 许可 | Apache 2.0（仓库内 LICENSE） |
| 使用的 SDK | Strands Agents SDK（强制项，务必勾选/写明） |
| Demo 视频 | 录制后填入公开链接 |
| 一句话简介 | 把会议转录端到端变成带责任人与截止日期的结构化纪要，每周替知识工作者省下半小时机械劳动。 |
